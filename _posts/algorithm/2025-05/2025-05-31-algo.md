---
title: "[JAVA] 백준 Silver IV - 스택 2(28278)"
date: 2025-05-31 12:36:50 +0900
categories: [Algorithm, 백준]
tags: [TIL, Algorithm]
---
# 코딩테스트 문제 풀이

## 📘 문제 제목
[백준 - 스택 2](https://www.acmicpc.net/problem/28278)

### 🧠 문제 이해
![img.png](/assets/img/algorithm/2025-05-31.png)

---

### ✏️ 슈도 코드

```plaintext
switch (num):
  case 1: push
  case 2: pop or -1
  case 3: size
  case 4: 0 or 1
  case 5: peek or -1
```

---

### ✅ 전체 코드
```java
import java.util.Scanner;
import java.util.Stack;

public class Main {
  public static void main(String[] arg) {
    Scanner sc = new Scanner(System.in);
    int n = sc.nextInt();
    Stack<Integer> stack = new Stack<>();

    StringBuilder sb = new StringBuilder();
    for (int i = 0; i < n; i++) {
      int num = sc.nextInt();

      switch (num) {
        case 1:
          stack.push(sc.nextInt());
          break;
        case 2:
          if (!stack.isEmpty()) {
            sb.append(stack.pop()).append("\n");
          } else {
            sb.append(-1).append("\n");
          }
          break;
        case 3:
          sb.append(stack.size()).append("\n");
          break;
        case 4:
          if (!stack.isEmpty()) {
            sb.append(0).append("\n");
          } else {
            sb.append(1).append("\n");
          }
          break;
        case 5:
          if (!stack.isEmpty()) {
            sb.append(stack.peek()).append("\n");
          } else {
            sb.append(-1).append("\n");
          }
          break;
      }
    }
    System.out.println(sb.toString());
  }
}
```
